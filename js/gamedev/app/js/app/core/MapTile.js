goog.provide('elt.core.MapTile');

/**
 *
 * @type {number} x
 * @type {number} y
 * @type {number} type
 * @constructor
 */
elt.core.MapTile = function(x, y, type) {
    this.x = x;
    this.y = y;
    this.type = type;
};
